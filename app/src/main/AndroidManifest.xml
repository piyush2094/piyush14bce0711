<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.example.deepak.vshare"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS"/>
    <uses-permission android:name="android.permission.READ_PROFILE"/>
    <uses-permission android:name="android.permission.READ_CONTACTS"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
            android:name=".Signin"
            android:label="@string/title_activity_signin"
            android:parentActivityName=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.deepak.vshare.MainActivity"/>
        </activity>
        <activity
            android:name=".AddGame"
            android:label="@string/title_activity_add_game"
            android:parentActivityName=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.deepak.vshare.MainActivity"/>
        </activity>
        <activity
            android:name=".AddMovie"
            android:label="@string/title_activity_add_movie"
            android:theme="@style/AppTheme.NoActionBar"/>
        <activity
            android:name=".AddDATA"
            android:label="@string/title_activity_add_dat"
            android:theme="@style/AppTheme.NoActionBar"/>
        <activity
            android:name=".AddTVseries"
            android:label="@string/title_activity_add_tvseries"
            android:theme="@style/AppTheme.NoActionBar"/>
        <activity android:name=".AddSong">
        </activity>
    </application>

</manifest>
